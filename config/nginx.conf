# user www-data;  # Commented out for Windows filesystem compatibility
worker_processes auto;
pid /run/nginx.pid;
error_log /var/log/nginx/error.log;

events {
    worker_connections 1024;
}

http {
    # MIME types
    types {
        text/html                             html htm shtml;
        text/css                              css;
        text/xml                              xml;
        image/gif                             gif;
        image/jpeg                            jpeg jpg;
        application/javascript                js;
        application/json                      json;
        text/plain                            txt;
    }
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile off;  # Disable sendfile for Windows filesystem compatibility
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    # Enable gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json;

    server {
        listen 80;
        server_name localhost;
        
        root "/mnt/c/Users/avish/OneDrive/Desktop/User Story automation frontend/user-story-automation";
        index index.html;
        
        # Disable directory listing
        autoindex off;
        
        # Main location block - serve pages
        location / {
            root "/mnt/c/Users/avish/OneDrive/Desktop/User Story automation frontend/user-story-automation/pages";
            try_files $uri $uri/ /index.html;
            index index.html;
        }
        
        # Serve assets from root
        location /assets/ {
            root "/mnt/c/Users/avish/OneDrive/Desktop/User Story automation frontend/user-story-automation";
            expires 1y;
            add_header Cache-Control "public, immutable";
            access_log off;
        }
        
        # Serve static files with appropriate cache headers
        location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot)$ {
            root "/mnt/c/Users/avish/OneDrive/Desktop/User Story automation frontend/user-story-automation";
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
        
        # Proxy API requests to Flask backend
        location /api/ {
            proxy_pass http://127.0.0.1:5000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Increase timeout for long-running requests
            proxy_read_timeout 300s;
            proxy_connect_timeout 300s;
        }
        
        # Security headers
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
    }
}

